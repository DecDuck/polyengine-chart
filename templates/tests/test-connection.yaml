apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "polyengine-chart.fullname" . }}-test-connection"
  labels:
    {{- include "polyengine-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "polyengine-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
